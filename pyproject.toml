[project]
name = "one-feature-regression"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
license = { text = "MIT" }
dependencies = [
    "matplotlib>=3.11.1",
    "numpy>=2.5.1",
]
